# GPS COM Device Statuses

Этот документ описывает возможные состояния GPS COM устройства и короткие текстовые статусы для их обозначения.  
Статусы используются для логирования и отображения состояния работы приёмника.

---

## 📌 Список статусов

| Состояние устройства                  | Короткий статус | Описание                                                                 |
|---------------------------------------|-----------------|--------------------------------------------------------------------------|
| Нет связи с COM‑портом                | `Offline`       | Порт недоступен или устройство не подключено.                            |
| COM‑порт открыт, но данных нет        | `Idle`          | Порт доступен, но строки NMEA не поступают.                              |
| Приёмник включён, идёт поток NMEA     | `Active`        | Устройство передаёт данные, но фиксации может ещё не быть.               |
| Данные приходят, но фиксации нет      | `Searching`     | Приёмник ищет спутники, `$GPRMC` → `V`, `$GPGGA` → `Fix Quality = 0`.    |
| Есть 2D‑фиксация (широта/долгота)     | `Fix2D`         | Минимум 3 спутника, координаты определены без высоты.                    |
| Есть 3D‑фиксация (широта/долгота/высота) | `Fix3D`      | Минимум 4 спутника, координаты и высота определены.                      |
| DGPS‑фиксация (с поправками)          | `DGPS`          | Используются дифференциальные поправки, точность выше.                   |
| RTK‑фиксация (сантиметровая точность) | `RTK`           | Используется RTK‑режим, высокая точность.                                |
| Ошибка приёма/парсинга данных         | `Error`         | Строки повреждены или не соответствуют формату NMEA.                     |

---

## 📌 Логика переключения статусов

1. **Offline → Idle**: при открытии COM порта.  
2. **Idle → Active**: при появлении потока NMEA строк.  
3. **Active → Searching**: если данные приходят, но фиксации нет.  
4. **Searching → Fix2D/Fix3D/DGPS/RTK**: при успешной фиксации позиции.  
5. **Любое состояние → Error**: при некорректных данных.  

---

## 📌 Пример использования

```text
[17:56:01] Status: Offline
[17:56:05] Status: Idle
[17:56:07] Status: Active
[17:56:15] Status: Searching
[17:56:45] Status: Fix3D
