# Dependency Resolver Script v3.0

[![Version](https://img.shields.io/badge/version-3.0-green.svg)](https://github.com/) [![Python 3.8+](https://img.shields.io/badge/python-3.8%2B-blue.svg)](https://python.org/)

> **Автоматизированный скрипт для копирования и распаковки зависимостей в build-директории release и debug конфигураций**

## ✨ Что делает скрипт

1. **Сканирует** папки с зависимостями:
   - `deps_exe/` — копирует **в корень** release/debug
   - `deps_above_exe/` — копирует **на уровень выше** exe
   - `deps_only_for_debug/` — **только** в debug
   - `deps_only_for_release/` — **только** в release

2. **Автоматически распаковывает** `.7z` архивы
3. **Копирует** в `../` директории с именами содержащими `release`/`debug`

## 📁 Структура проекта
deps_resolver/  
├── deps/ # ← Корневая директория  
│ ├── deps_exe/ # DLL, exe для корня build  
│ ├── deps_above_exe/ # Библиотеки на уровень выше  
│ ├── deps_only_for_debug/ # Только для debug  
│ ├── deps_only_for_release/ # Только для release  
│ ├── copy_all_deps.py # ← СКРИПТ  
│ └── README.md # ← ЭТОТ ФАЙЛ  
├── ../build_release/ # ← Автоматически заполняется  
│ ├── release/  
│ └── debug/  
└── ../build_debug/  
├── release/  
└── debug/  


## 🚀 Быстрый старт
Двойной клик resolve_deps.bat запускает copy_all_deps.py  
Папку deps_resolver положить в папку с сорсами.


